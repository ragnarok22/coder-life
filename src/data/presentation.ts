/** Physical units and seconds. Presentation tuning is independent from the day clock. */
export const PLAYER_MOTION = {
  walkSpeed: 3.2,
  runSpeed: 4.9,
  acceleration: 26,
  deceleration: 34,
  walkRotationSpeed: 11,
  runRotationSpeed: 9,
  reversalRotationSpeed: 8,
  gravity: 20,
  terminalVelocity: 20,
  radius: 0.3,
  halfHeight: 0.48,
  colliderOffset: 0.025,
  stepHeight: 0.3,
  stepWidth: 0.2,
  snapDistance: 0.35,
  animationBlend: 12,
  idleThreshold: 0.06,
} as const;
export const THIRD_PERSON_CAMERA = {
  distance: 6.2,
  minDistance: 2.6,
  maxDistance: 8.5,
  pitch: 0.48,
  minPitch: 0.15,
  maxPitch: 1.1,
  lookHeight: 1.32,
  targetSmoothing: 15,
  positionSmoothing: 14,
  rotationSmoothing: 18,
  distanceRecovery: 5,
  collisionRadius: 0.24,
  collisionMargin: 0.06,
  closeDistance: 2.4,
  confinedBlend: 12,
  confinedAngles: [1.02, 1.25, 1.43, 1.53, 1.56],
  dialogueDistance: 0.92,
  fov: 50,
  near: 0.08,
} as const;
export const OFFICE_PRESENTATION = {
  npcCount: 12,
  maxNpcs: 15,
  aiInterval: 1 / 30,
  farAiInterval: 1 / 12,
  farDistance: 14,
  animationDistance: 18,
  perceptionInterval: 0.35,
  pathInterval: 1.1,
  smallTalkSeconds: [3, 6] as const,
  smallTalkCooldown: 35,
} as const;
